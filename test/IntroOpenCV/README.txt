IntroOpenCV

A default hello-world type project that does nothing except load a standard view and link against OpenCV framework. Use it to test if you can compile an Xcode project with OpenCV framework included.